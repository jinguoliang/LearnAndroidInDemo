<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppBar" parent="@style/ThemeOverlay.AppCompat">
        <item name="selectableItemBackground">@null</item>
    </style>
</resources>
